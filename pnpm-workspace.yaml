packages:
  - '.'

# Approve install/postinstall build scripts (pnpm blocks the rest by default for
# supply-chain safety). esbuild needs its native binary built on install.
allowBuilds:
  esbuild: true
  es5-ext: true
